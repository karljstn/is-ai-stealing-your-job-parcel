export type Viewport = {
    height: number
    width: number
}

export type PARAMS = {
    maxFPS: number
    readyFPS: boolean
    averageFPS: number
    arrFPS: number[]
    scoreFPS: number
    testValue: number
    viewport: Viewport
}